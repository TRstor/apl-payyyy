rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // #20: Block all client SDK access — only Admin SDK (server) can read/write
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
